{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf build && cd ../client && yarn build && mv -f build ../server && rm -rf build"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.17.3",
    "morgan": "^1.9.1",
    "uuid": "^3.3.2"
  }
}
